#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main()
{
	int num;
	int N, X = 0;
	cin >> N >> X;
	vector<int> A ;
	for (int i = 0; i < N; i++)
	{
		cin >> num;
		A.push_back(num);
	}
	for (int j = 0; j < N; j++)
	{
		if (X > A[j])
		{
			cout << A[j];
			cout << " ";
		}
	}
	return 0;
}
