#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main()
{
	vector<int> arr;
	int num;
	cin >> num;
	for (int i = 0; i < num; i++)
	{
		int temp = 0;
		cin >> temp;
		arr.push_back(temp);
	}
	sort(arr.begin(), arr.end());
	for (int j = 0; j < arr.size(); j++)
	{
		cout << arr[j] << '\n';
	}
	return 0;
}
